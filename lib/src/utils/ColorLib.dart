import 'package:flutter/material.dart';

// rgb(50,165,113)
class ColorLib {
  static Color primary = Color.fromRGBO(17, 54, 35, 1); // dark green
  static Color secondary = Color.fromRGBO(50, 165, 113, 1); // light screen
  static Color primaryLight =
      Color.fromRGBO(122, 149, 149, 0.7); // inactive primary
  static Color text = Color.fromRGBO(17, 54, 35, 1);

  static Color cardBackground = Color.fromRGBO(220, 235, 239, 0.45);
  // static HSLColor cardBackground = HSLColor.fromAHSL(1.0, 193.0, 0.8, 0.94);
}
